// const getTotalValue = () => {
//   const soma = productsListCart.reduce((acc, curr) => {
//     acc += (+(curr.price) * curr.quantity);
//     return acc;
//   }, 0);
//   return soma;
// };

// export default getTotalValue;
